import { CredtCardPipe } from './credt-card.pipe';

describe('CredtCardPipe', () => {
  it('create an instance', () => {
    const pipe = new CredtCardPipe();
    expect(pipe).toBeTruthy();
  });
});
