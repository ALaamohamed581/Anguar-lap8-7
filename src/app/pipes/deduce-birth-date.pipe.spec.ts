import { DeduceBirthDatePipe } from './deduce-birth-date.pipe';

describe('DeduceBirthDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DeduceBirthDatePipe();
    expect(pipe).toBeTruthy();
  });
});
